spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/em-anno
      aliases:
        microservicekey-em-annotation-app: S2S_KEY
        AppInsightsInstrumentationKey: APPINSIGHTS_INSTRUMENTATIONKEY
        anno-POSTGRES-PASS: SPRING_DATASOURCE_PASSWORD
