spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/em-anno
      aliases:
        microservicekey-em-annotation-app: S2S_KEY
        AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        anno-POSTGRES-PASS: spring.datasource.password
